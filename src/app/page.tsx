import { Home } from "@/components/home/Home";

/** The game opens on Planet 01. Swipe for the next world; the map, fleet and market are one tap away. */
export default function HomePage() {
  return <Home />;
}
